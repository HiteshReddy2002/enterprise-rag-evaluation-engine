version: "3.8"

services:
  rag-service:
    build: .
    container_name: llm_rag_service
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=production
      - LOG_LEVEL=INFO
      - LLM_PROVIDER=mock
    volumes:
      - ./data:/app/data
    restart: unless-stopped
